server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booking
    username: root
    password: password

  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
